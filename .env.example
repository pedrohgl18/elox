# Exemplo de variáveis de ambiente - NÃO COMMITAR segredos reais
# Copie para .env.local e preencha os valores no seu ambiente local

# NextAuth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=changeme-generated-secret

# App URLs (dinâmico por padrão, apenas se precisar forçar)
# BASE_URL=https://seu-dominio-netlify.app

# Credenciais OAuth (se usar provedores futuramente)
# GITHUB_ID=
# GITHUB_SECRET=
# GOOGLE_ID=
# GOOGLE_SECRET=

# Banco de dados (planejado)
# DATABASE_URL=

# Supabase (definir via painel web)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Nunca expor no cliente: usar somente em server functions
SUPABASE_SERVICE_ROLE_KEY=

# YouTube Data API v3 (usado para coletar métricas públicas dos vídeos)
# Defina no Netlify (Site Settings > Build & deploy > Environment) como YOUTUBE_API_KEY
# Para desenvolvimento local, você pode definir aqui temporariamente.
YOUTUBE_API_KEY=
