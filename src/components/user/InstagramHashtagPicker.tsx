// Este componente foi descontinuado com a remoção de login/conexões sociais.
// Mantemos um stub para evitar erros de import acidentais.
export function InstagramHashtagPicker(_props: { onPick: (item: any) => void }) {
  return null;
}
