// Stub seguro: recurso de Contas Sociais desativado por requisito.
// Mantemos o export para não quebrar imports existentes, mas não renderiza nada.
'use client';

import React from 'react';

export function SocialAccountsManager() {
  return null;
}
